[tool.uv]
workspace = { members = ["backend"] }

[project]
name = "triguard-ai-monorepo"
version = "3.0.0"
description = "TriGuard AI Monorepo root"
requires-python = ">=3.11"
dependencies = [
    "motor>=3.7.1",
]
